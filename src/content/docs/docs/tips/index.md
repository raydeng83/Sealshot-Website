---
title: Tips & tricks
description: Tips for every stage of a capture — capture, encrypt, refine, organize, deliver.
---

Here are some tips and tricks to get more out of Sealshot. They follow the
same five stages as the [workflows](/docs/workflows/), so a tip lives where
you'd use it:

- **[Capture](/docs/tips/capture/)** — selection tricks, frozen screens, and
  the modes people forget
- **[Encrypt](/docs/tips/encrypt/)** — recovery codes, what's covered, and
  auto-lock
- **[Refine](/docs/tips/refine/)** — annotation tools, image cleanup, and
  the smart features
- **[Organize](/docs/tips/organize/)** — search, filters, collections, and
  the Trash
- **[Deliver](/docs/tips/deliver/)** — exports, encrypted packages, and
  what's in the file you send

## Set these once

Three settings that change how every capture behaves. Worth a minute now.

- **Where captures go.** [Settings →
  Capture](/docs/guide/settings/#capture) decides whether a finished capture
  goes to the **Clipboard**, a **File**, or **Both** (the default), and sets
  the filename format. Clipboard-only skips the editor entirely.
- **Where files land.** The save folder lives in [Settings →
  General](/docs/guide/settings/#general), alongside how long the Trash
  keeps deleted captures.
- **Make the shortcuts yours.** Rebind any command in [Settings →
  Shortcuts](/docs/guide/settings/#shortcuts) — the recorder refuses a
  combination that's already taken, so you can't create a silent conflict.

:::tip[Keep exploring]
Ready to put these together? The [workflows](/docs/workflows/) show how the
pieces combine to get real jobs done, and the
[examples](/docs/workflows/recipes/) name eleven specific ones.
:::
